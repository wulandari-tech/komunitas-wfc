<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>create new account for komunitas wfc</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap');
        :root {
            --primary-color: #007bff; /* Biru modern */
            --secondary-color: #6c757d; /* Abu-abu netral */
            --success-color: #28a745; /* Hijau untuk sukses */
            --danger-color: #dc3545; /* Merah untuk error */
            --light-color: #f8f9fa; /* Putih keabuan */
            --dark-color: #343a40; /* Hitam keabuan */
            --background-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            --input-bg: rgba(255, 255, 255, 0.1);
            --input-border: rgba(255, 255, 255, 0.3);
            --text-light: #e0e0e0;
            --text-dark: #333;
            --card-bg: rgba(0, 0, 0, 0.3); /* Semi-transparent card */
        }
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background: var(--background-gradient);
            color: var(--text-light);
            font-size: 16px;
            line-height: 1.6;
            padding: 20px;
        }
        .auth-container {
            background: var(--card-bg);
            padding: 40px 50px;
            border-radius: 15px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.3);
            width: 100%;
            max-width: 480px;
            text-align: center;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255,255,255,0.1);
        }
        .auth-container h1 {
            font-size: 2.5em;
            color: var(--light-color);
            margin-bottom: 10px;
            font-weight: 600;
            letter-spacing: 1px;
        }
        .auth-container p.subtitle {
            color: var(--text-light);
            margin-bottom: 30px;
            font-size: 1.1em;
        }
        .form-group {
            margin-bottom: 25px;
            text-align: left;
            position: relative;
        }
        .form-group .icon {
            position: absolute;
            left: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--text-light);
            opacity: 0.7;
            font-size: 1.1em;
        }
        .form-group input {
            width: 100%;
            padding: 15px 15px 15px 50px; /* Padding for icon */
            background: var(--input-bg);
            border: 1px solid var(--input-border);
            border-radius: 8px;
            font-size: 1em;
            color: var(--text-light);
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
        }
        .form-group input::placeholder {
            color: rgba(224, 224, 224, 0.7);
        }
        .form-group input:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(0, 123, 255, 0.25);
        }
        .btn-submit {
            width: 100%;
            padding: 15px;
            background: var(--primary-color);
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 1.1em;
            font-weight: 600;
            letter-spacing: 0.5px;
            transition: background-color 0.3s ease, transform 0.2s ease;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .btn-submit i {
            margin-right: 8px;
        }
        .btn-submit:hover {
            background: #0056b3; /* Darker shade of primary */
            transform: translateY(-2px);
        }
        .btn-submit:active {
            transform: translateY(0);
        }
        .message {
            padding: 12px 15px;
            border-radius: 8px;
            margin-bottom: 25px;
            text-align: center;
            font-size: 0.95em;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .message i {
            margin-right: 10px;
            font-size: 1.2em;
        }
        .error-message {
            color: #f8d7da;
            background-color: rgba(220, 53, 69, 0.3); /* Semi-transparent red */
            border: 1px solid var(--danger-color);
        }
        .success-message {
            color: #d4edda;
            background-color: rgba(40, 167, 69, 0.3); /* Semi-transparent green */
            border: 1px solid var(--success-color);
        }
        .auth-link {
            margin-top: 30px;
            display: block;
            color: var(--text-light);
            text-decoration: none;
            font-size: 0.95em;
            transition: color 0.3s ease;
        }
        .auth-link:hover {
            color: var(--primary-color);
            text-decoration: underline;
        }
        .auth-link i {
            margin-right: 5px;
        }
    </style>
</head>
<body>
    <div class="auth-container">
        <h1><i class="fas fa-user-plus"></i> buat Akun</h1>
        <p class="subtitle">create new account!</p>

        <% if (error) { %>
            <p class="message error-message"><i class="fas fa-exclamation-circle"></i> <%= error %></p>
        <% } %>
        <% if (success) { %>
            <p class="message success-message"><i class="fas fa-check-circle"></i> <%= success %></p>
        <% } %>

        <form action="/register" method="POST">
            <div class="form-group">
                <i class="fas fa-user icon"></i>
                <input type="text" id="username" name="username" placeholder="Nama Pengguna Unik" required>
            </div>
            <div class="form-group">
                <i class="fas fa-lock icon"></i>
                <input type="password" id="password" name="password" placeholder="Password (min. 6 karakter)" required minlength="6">
            </div>
            <div class="form-group">
                <i class="fas fa-shield-alt icon"></i>
                <input type="password" id="confirmPassword" name="confirmPassword" placeholder="Konfirmasi Password" required>
            </div>
            <button type="submit" class="btn-submit"><i class="fas fa-paper-plane"></i></button>
        </form>
        <a href="/login" class="auth-link"><i class="fas fa-sign-in-alt"></i> Sudah punya akun? Login di sini</a>
    </div>
</body>
</html>