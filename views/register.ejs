<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Buat Akun Baru - Diskusi</title>
    <%- include('partials/common_head_content') %>
     <style>
        body { display: flex; justify-content: center; align-items: center; min-height: 100vh; background: var(--background-gradient, linear-gradient(135deg, #667eea 0%, #764ba2 100%)); color: var(--text-light, #e0e0e0); padding: 20px; }
        .auth-container { background: var(--card-bg, rgba(0,0,0,0.3)); padding: 40px 50px; border-radius: 15px; box-shadow: 0 15px 35px rgba(0,0,0,0.3); width: 100%; max-width: 480px; text-align: center; backdrop-filter: blur(10px); border: 1px solid rgba(255,255,255,0.1); }
        .auth-container h1 { font-size: 2.2em; color: var(--light-color, #f8f9fa); margin-bottom: 10px; font-weight: 600; letter-spacing: 1px; }
        .auth-container p.subtitle { color: var(--text-light, #e0e0e0); margin-bottom: 30px; font-size: 1em; }
        .form-group { margin-bottom: 20px; text-align: left; position: relative; } /* Adjusted margin */
        .form-group .icon { position: absolute; left: 18px; top: 50%; transform: translateY(-50%); color: var(--text-light, #e0e0e0); opacity: 0.7; font-size: 1.1em; }
        .form-group input { width: 100%; padding: 15px 15px 15px 50px; background: var(--input-bg, rgba(255,255,255,0.1)); border: 1px solid var(--input-border, rgba(255,255,255,0.3)); border-radius: 8px; font-size: 1em; color: var(--text-light, #e0e0e0); }
        .form-group input::placeholder { color: rgba(224, 224, 224, 0.7); }
        .form-group input:focus { outline: none; border-color: var(--primary-color, #007bff); box-shadow: 0 0 0 3px rgba(0, 123, 255, 0.25); }
        .btn-submit { width: 100%; padding: 15px; background: var(--success-color, #28a745); color: white; border: none; border-radius: 8px; cursor: pointer; font-size: 1.1em; font-weight: 600; letter-spacing: 0.5px; transition: background-color 0.3s ease, transform 0.2s ease; display: flex; justify-content: center; align-items: center; }
        .btn-submit i { margin-right: 8px; }
        .btn-submit:hover { background: #218838; transform: translateY(-2px); }
        .auth-link { margin-top: 25px; display: block; color: var(--text-light, #e0e0e0); text-decoration: none; font-size: 0.95em; } /* Adjusted margin */
        .auth-link:hover { color: var(--primary-color, #007bff); text-decoration: underline; }
        .auth-link i { margin-right: 5px; }
    </style>
</head>
<body>
    <div class="auth-container">
        <h1><i class="fas fa-user-plus"></i> Buat Akun</h1>
        <p class="subtitle">Bergabunglah dalam diskusi realtime paling canggih!</p>

        <% if (locals.error) { %><div class="alert alert-danger"><i class="fas fa-exclamation-circle"></i> <%= error %></div><% } %>
        <% if (locals.success) { %><div class="alert alert-success"><i class="fas fa-check-circle"></i> <%= success %></div><% } %>

        <form action="/register" method="POST">
            <div class="form-group">
                <i class="fas fa-user icon"></i>
                <input type="text" id="username" name="username" placeholder="Nama Pengguna Unik" required value="<%= locals.oldInput && oldInput.username ? oldInput.username : '' %>">
            </div>
             <div class="form-group">
                <i class="fas fa-id-badge icon"></i>
                <input type="text" id="displayName" name="displayName" placeholder="Nama Tampilan (Opsional)" value="<%= locals.oldInput && oldInput.displayName ? oldInput.displayName : '' %>">
            </div>
            <div class="form-group">
                <i class="fas fa-envelope icon"></i>
                <input type="email" id="email" name="email" placeholder="Email (Opsional)" value="<%= locals.oldInput && oldInput.email ? oldInput.email : '' %>">
            </div>
            <div class="form-group">
                <i class="fas fa-lock icon"></i>
                <input type="password" id="password" name="password" placeholder="Password (min. 6 karakter)" required minlength="6">
            </div>
            <div class="form-group">
                <i class="fas fa-shield-alt icon"></i>
                <input type="password" id="confirmPassword" name="confirmPassword" placeholder="Konfirmasi Password" required>
            </div>
            <button type="submit" class="btn-submit"><i class="fas fa-paper-plane"></i> Daftar Sekarang</button>
        </form>
        <a href="/login" class="auth-link"><i class="fas fa-sign-in-alt"></i> Sudah punya akun? Login di sini</a>
    </div>
    <%- include('partials/common_scripts') %>
</body>
</html>
